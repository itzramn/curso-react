export const FirstApp = ({title, subTitle}) => {
  // console.log(props);
  return (
    <>
      <h1>{title}</h1>
      {/* <code>{JSON.stringify(newMesage)}</code> */}
      <p>{subTitle + 1}</p>
    </>
  );
};
